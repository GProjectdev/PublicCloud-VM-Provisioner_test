# Replace these placeholders before applying this Secret. Alternatively, omit
# both keys when the controller Pod already has an IAM role.
apiVersion: v1
kind: Secret
metadata:
  name: aws-node-credentials
  namespace: default
type: Opaque
stringData:
  awsAccessKeyId: "REPLACE_ME"
  awsSecretAccessKey: "REPLACE_ME"
